# This is a test file for testing require statement position requirements

# Define a variable first
message = "Hello from main file!"

# This require statement is not at the top of the file, so it should cause an error
require "./simple_module"

# Use the variable from the required file
puts "Required message: " + message