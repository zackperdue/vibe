# This is a test file with multiple require statements at the top

# First require statement
require "./simple_module"

# Second require statement
require "./just_message"

# Use variables from both required files
puts "Message from simple_module: " + message

# After both requires at the top, we should be able to add code
x = 10
puts "Value of x: " + x