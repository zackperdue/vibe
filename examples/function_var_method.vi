# Function result to variable, then method call

# Define a simple function
def greeting(): string do
  return "hello world"
end

# Call the function and assign the result to a variable
result = greeting()

# Call a method on the variable
puts result.upcase()